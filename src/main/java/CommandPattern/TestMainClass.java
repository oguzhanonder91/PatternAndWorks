package CommandPattern;

public class TestMainClass {
    public static void main(String[] args) {
        RemoteControl remoteControl = new RemoteControl();
        remoteControl.test(0);
        remoteControl.test(1);
    }
}
